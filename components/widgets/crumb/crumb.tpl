<div class="col s12 m8 l9 crumb-warp">
    <nav>
        <div class="nav-wrapper crumb-bg">
            <div class="col s12 beard-wrap">
                <a href="{{item.url}}" class="breadcrumb" v-for="item in result.list">{{item.name}}</a>
            </div>
        </div>
    </nav>
</div>